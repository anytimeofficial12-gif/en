# Environment Configuration
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000

# Database (PostgreSQL)
# Example for common providers (pick one and set as DATABASE_URL):
# - Local:           postgresql://postgres:postgres@localhost:5432/anytime
# - Neon/Render:     postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
# - Railway:         postgresql://USER:PASSWORD@HOST:PORT/railway
DATABASE_URL=

# CORS Configuration (comma-separated list)
# ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-url.vercel.app
# When deploying on Vercel with same origin, you can allow all:
# ALLOW_ALL_ORIGINS=true
